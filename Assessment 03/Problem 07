// digit0 and digit1 are not identical
#include<stdio.h>
int main(){
    int num,digit0,digit1;
    scanf("%d",&num);
    digit0=num%10;
    digit1=num/10;
    if(digit0!=digit1){
        printf("success");
    }
    else{
        printf("failure");
    }
}
