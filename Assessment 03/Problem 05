//if the number is greater than or equal to 50 print success else failure
#include<stdio.h>
int main(){
    int num;
    scanf("%d",&num);
    if(num>=50){
        printf("Success");
    }
    else{
        printf("Failure");
    }
    return 0;
}
